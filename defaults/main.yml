---
motd_files:
  - issue
  - issue.net
  - motd

# IPs addresses on which sshd must listen
sshd_ipv4: "{{ ansible_facts['default_ipv4'].address }}"
sshd_ipv6: "{{ ansible_facts['default_ipv6'].address }}"

# To add users and groups limitations
# sshd_allowed_users:
# sshd_allowed_groups:
